import React from 'react'
import { View } from 'react-native'

const GapSpace = ({gapHeight, gapWidth}) => {
    return (
        <View style={{width: gapWidth, height: gapHeight}}>
    
        </View>
    )
}

export default GapSpace
